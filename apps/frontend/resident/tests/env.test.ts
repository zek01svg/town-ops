import { expect, test } from "vitest";

test("frontend environment is correctly configured", () => {
  // Basic smoke test to ensure Vitest has at least one test file
  // and the environment variables/Setup are working.
  expect(true).toBe(true);
});
