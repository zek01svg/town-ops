{
  "name": "@townops/assignment-atom",
  "version": "0.1.0",
  "description": "Atom service for assigning jobs in TownOps",
  "main": "src/main.py",
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --port 5003 --host 127.0.0.1",
    "start": "uv run uvicorn src.main:app --port 5003 --host 127.0.0.1"
  }
}
