FROM oven/bun:canary-slim AS runner
WORKDIR /app

# Copy the standalone bun bundle generated by `bun run build`
COPY build ./build
# migrate.js reads ./src/database/drizzle at runtime (see src/database/migrate.ts)
COPY src/database/drizzle ./src/database/drizzle

EXPOSE 5004
CMD ["sh", "-c", "bun build/database/migrate.js && bun build/index.js"]
